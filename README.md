# react_rails_env

### dockerの起動
```bash
docker-compose build
docker-compose up -d
```

### rails
```bash
docker-compose run api bash
```
```ruby
rails new . -f -d mysql --api
rails db:create
```
###
